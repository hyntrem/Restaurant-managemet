server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location = / {
        return 302 /templates/staff/login.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}