server {
    listen 80;
    root /usr/share/nginx/html;

    location = / {
        try_files /templates/customer/home.html =404;
    }

    location / {
        try_files /templates/customer$uri $uri =404;
    }

    location /static/ {
        try_files $uri =404;
    }
}
